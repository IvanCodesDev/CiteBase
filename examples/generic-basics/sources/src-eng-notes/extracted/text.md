# 工程实践笔记

## 幂等性
幂等操作重复执行的效果与执行一次相同，是安全重试的前提。
网络请求的幂等性通常靠客户端生成的幂等键实现。

## 重试策略
指数退避在每次失败后成倍拉长等待时间，配合抖动可避免重试风暴。
无上限的重试会放大故障，必须设置最大尝试次数与总时长预算。

## 缓存
缓存雪崩指大量缓存同时失效导致请求全部打到后端存储。
给过期时间加随机抖动与热点数据预热是缓解缓存雪崩的常用手段。

## 内容寻址
内容寻址存储用内容哈希作为标识，天然去重且可校验完整性。
